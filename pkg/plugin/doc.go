// Package plugin provides a plugable infrastructure for native envel bindings
// Plugins are loaded when envel is started and can preload code to be exposed to
// the event loop / lua LState
package plugin
