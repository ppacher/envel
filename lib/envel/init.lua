return {
    signal = require("envel.signal"),
    timer = require("envel.timer"),
    reader = require("envel.reader"),
    spawn = require("envel.spawn"),
    utils = require("envel.utils"),
    device = require("envel.device"),
}